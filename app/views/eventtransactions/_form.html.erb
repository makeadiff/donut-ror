<%= form_for(@event_transaction) do |f| %>
  <% if @event_transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_transaction.errors.count, "error") %> prohibited this event_transaction from being saved:</h2>

      <ul>
      <% @event_transaction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
