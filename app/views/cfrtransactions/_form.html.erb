<%= form_for(@poctransaction) do |f| %>
  <% if @poctransaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poctransaction.errors.count, "error") %> prohibited this poctransaction from being saved:</h2>

      <ul>
      <% @poctransaction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
