
<%= form_for(@state) do |f| %>
  <% if @state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
      <% @state.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="grid4">
      <%= f.label "Region", :class => 'col2' %>
      <%= f.text_field :name, :class => 'col6', :id => "regionText", :maxlength => 30 %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit "Create Region", :id => 'state-button' %>
  </div>
<% end %>
 